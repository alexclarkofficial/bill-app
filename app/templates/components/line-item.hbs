{{#draggable-item lineItemID=lineItem.id cartID=lineItem.cart}}
  <div class="cell">{{lineItem.qty}}</div>
  <div class="cell">{{lineItem.name}}</div>
  <div class="cell">{{lineItem.price}}</div>
  <div class="cell">{{lineItem.extendedPrice}}</div>
{{/draggable-item}}
